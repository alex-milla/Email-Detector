# Email Malware Detector — dependencias Python
# Instalar con: pip install -r requirements.txt

# --- Core ML ---
scikit-learn
pandas
numpy
joblib
imbalanced-learn          # imblearn

# --- Modelos opcionales (GPU opcional en xgboost/lightgbm) ---
xgboost
lightgbm
catboost

# --- Web ---
flask
flask-cors
flask-limiter             # MED-10: rate limiting en login
gunicorn
werkzeug
bcrypt

# --- Conectores de correo ---
# IMAP: se usa imaplib de la biblioteca estándar (mailbox_connector.py)
msal                        # Microsoft 365 / Azure AD
google-auth
google-auth-oauthlib
google-api-python-client    # Gmail OAuth

# --- Utilidades ---
requests
python-dotenv
pyyaml
packaging

# --- Detección de QR (v2.0) ---
pyzbar                    # decoder primario de QR (requiere libzbar0 del sistema)
opencv-python-headless    # decoder fallback de QR (~60 MB, sin GUI)
Pillow                    # manipulación de imágenes
beautifulsoup4            # parsing de meta-refresh
playwright                # detección de redirecciones JS (~300 MB con chromium)

# --- Monitorización (Fase 5) ---
prometheus-client          # métricas /metrics para Prometheus

